name: Snake game

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set up repository
      uses: actions/checkout@v2

    - name: Play Snake
      uses: actions/gh-snake@v1
